
/*public class TLEx2_2V2 {

	public static void main(String[] args) {
		DEBUT
		//Variables locales
		Entier : arr [] <= [ 15, 25, 0, 35, 48 , 2, 0, 89, 100, 7]
		Entier : nbrfind <= 0
		//Instructions
		nbrFind = getUser_IntInput(« Entrez la valeur entière à vérifier dans le tableau »)
		afficher(findValueInArray(nbrFind, arr)? «la valeur » + nbrFind + « se trouve dans le tableau » : « la valeur » + nbrFind + « ne se trouve pas dans le tableau »)

		Fonction findValueInArray(entier nbr, entier array) booleen :
		DEBUT
		//Variables locales
		Entier i = 0
		//Instructions
		TANT QUE (i < taille de array)
			SI(nbr = array[i])ALORS
				RETOURNE true
			SINON
			FIN SI
			I++
		FIN TANT QUE
		RETOURNE false
		FIN findValueInArray

		(nbr = array[i]) ? RETOURNE true : i++



		Fonction findValueInArray(entier nbr, entier array[]) entier :
		DEBUT
		//Variables locales
		Entier i <= 0
		//Instructions
		TANT QUE (i < taille de array)
			SI(nbr = array[i])ALORS
				RETOURNE 
			FIN SI
			I++
		FIN TANT QUE
		RETOURNE -1
		FIN findValueInArray


	}

}*/
